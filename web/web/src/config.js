// Compile service URL. Hardcoded to localhost for now -- becomes an env
// variable once this gets deployed anywhere beyond local development.
export const COMPILE_SERVICE_URL = 'http://localhost:4000';
